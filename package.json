{
  "name": "json-schema",
  "private": false,
  "version": "3.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "elm-live src/Main.elm --output=elm.js --open",
    "test": "elm-test --watch",
    "deploy": "gh-pages -d dist -a"
  },
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-live": "^2.7.4",
    "elm-test": "^0.18.7"
  },
  "dependencies": {
    "gh-pages": "^1.0.0"
  }
}
